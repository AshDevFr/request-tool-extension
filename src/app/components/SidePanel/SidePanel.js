/* @flow */
import React from 'react';
import './SidePanel.less';

export default class SidePanel extends React.Component {
  render() {
    return (
      <div>

      </div>
    );
  }

  static propTypes = {

  };
}
